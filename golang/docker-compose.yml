version: '3.8'

services:
  app-go:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - DB_PATH=/data/alexa.db
    env_file:
      - ../.env
    volumes:
      - alexa_data:/data

volumes:
  alexa_data:

version: '3.8'

services:
  app-go:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    env_file:
      - ../.env
    volumes:
      - ../volume/data:/data
